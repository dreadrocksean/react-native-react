import { defaultStyles } from "../mobileUtils";

export default `
  ${defaultStyles}
  margin: 0;
  padding: 0;
  color: #000;
  font-size: 16px;
  border: none;
  box-shadow: none;
  text-shadow: none;
  border: none;
`;
